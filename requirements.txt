# Pinned versions — update with care, test after any bump
# Base image (pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime) provides:
#   torch==2.5.1, torchaudio==2.5.1, numpy, etc.

# Model library
qwen-tts>=0.1.0

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
uvloop==0.21.0
httptools==0.6.4
python-multipart==0.0.12
orjson==3.10.12
websockets==13.1
h2==4.1.0
aiohttp>=3.9.0

# Audio processing
soundfile==0.12.1
pydub==0.25.1
scipy==1.14.1

# Model loading
accelerate==1.12.0

# Logging
loguru>=0.7.0

# Optional quantization (required only when QUANTIZE=int8 or QUANTIZE=fp8)
# Installed separately in Dockerfile with || true — keep out of main deps
# to avoid version conflicts with base torch (bitsandbytes, torchao)
